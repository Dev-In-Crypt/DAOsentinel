node_modules/
.next/
out/
build/
dist/
.env
.env.local
.env*.local
*.log
.DS_Store
.vercel
.turbo
coverage/
playwright-report/
test-results/
drizzle/meta/
*.tsbuildinfo

# Internal agent workflow docs — local only, never pushed to the public repo.
/AGENTS.md
/ROADMAP.md
/TODO.md
/STATE.md
/VERIFY.md
/ARCHITECTURE.md
/SECURITY_NOTES.md
/DECISIONS.md
/RUNS/
# The slash-command definitions that drive the workflow above (dao-daily,
# dao-session, dao-setup-review, dao-roadmap-review, dao-next-feature-plan).
# Same category as the docs they operate on — internal process, not product.
/.claude/

# Internal business/monetization planning — pricing, sales mechanics, and
# positioning are not appropriate for a public MIT repo, unlike docs/specs/
# (which covers open governance-monitoring features).
/MONETIZATION.md

# Local demo recordings — large binaries, never belong in git
*.mp4
*.mov

# Client-facing sales samples and outreach drafts — pricing/positioning,
# not for the public MIT repo. Same category as MONETIZATION.md.
/OUTREACH/
